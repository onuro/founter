import { DRBFetcherLayout } from '@/components/drb-fetcher/DRBFetcherLayout';

export const metadata = {
  title: 'DRB Fetcher | Founter',
  description: 'Extract images from any webpage using Crawl4AI',
};

export default function DRBFetcherPage() {
  return <DRBFetcherLayout />;
}
