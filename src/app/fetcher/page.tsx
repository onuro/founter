import { FetcherLayout } from '@/components/fetcher/FetcherLayout';

export const metadata = {
  title: 'Fetcher | Founter',
  description: 'Extract images and content from any webpage using Crawl4AI',
};

export default function FetcherPage() {
  return <FetcherLayout />;
}
